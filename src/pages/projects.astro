---
import BaseHead from '../components/BaseHead.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const focusCategories = [
	{ label: 'Security', slug: 'security' },
	{ label: 'Agent', slug: 'agent' },
	{ label: 'Network', slug: 'network' },
	{ label: 'Development', slug: 'development' },
];

const profile = {
	name: 'Yida',
	role: 'Exploring Secure Intelligent Systems',
	intro: 'This blog is a place for research notes, technical explorations, and ongoing projects.',
	github: 'https://github.com/Yodasama',
	email: 'yidaaa2269@163.com',
};

const projects = [
	{
		name: 'FedEdgeSim',
		description: '面向边缘场景的层次化联邦学习实验与仿真框架。',
		href: 'https://github.com/Yodasama',
		status: 'Active',
		categories: ['agent', 'development'],
		tags: ['Federated Learning', 'Simulation', 'Edge AI'],
	},
	{
		name: 'Research Notes',
		description: '整理论文阅读、实验记录和工程复盘的知识库。',
		href: '/blog',
		status: 'Growing',
		categories: ['security'],
		tags: ['Research', 'Security', 'Notes'],
	},
	{
		name: 'Blog System',
		description: '基于 Astro、GitHub 与 Cloudflare Pages 的个人博客。',
		href: '/',
		status: 'Live',
		categories: ['development'],
		tags: ['Astro', 'Cloudflare Pages', 'Frontend'],
	},
];
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`Projects | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>
	<body class="projects-page">
		<div class="site-shell">
			<aside class="profile-panel" aria-label="个人信息">
				<div class="profile-main">
					<img class="avatar" src="/images/panda.JPG" width="180" height="180" alt={`${profile.name} avatar`} />
					<h1>{profile.name}</h1>
					<p>{profile.role}</p>
				</div>

				<section class="panel-section">
					<h2>About Me</h2>
					<p>{profile.intro}</p>
				</section>

				<section class="panel-section">
					<h2>Focus</h2>
					<ul class="focus-list">
						{
							focusCategories.map((category) => (
								<li>
									<button class="focus-button" type="button" data-filter={category.slug}>
										<span>{category.label}</span>
									</button>
								</li>
							))
						}
					</ul>
				</section>

				<section class="panel-section connect-section">
					<h2>Connect</h2>
					<div class="contact-list">
						<a href={profile.github} target="_blank" rel="noreferrer">
							<span class="contact-icon" aria-hidden="true">
								<svg viewBox="0 0 24 24">
									<path
										d="M12 2.5a9.5 9.5 0 0 0-3 18.52c.48.08.66-.2.66-.46v-1.7c-2.7.6-3.27-1.16-3.27-1.16-.44-1.12-1.08-1.42-1.08-1.42-.88-.6.07-.6.07-.6.98.07 1.5 1.02 1.5 1.02.88 1.5 2.3 1.06 2.86.8.09-.64.34-1.06.62-1.3-2.16-.25-4.43-1.08-4.43-4.8 0-1.06.38-1.93 1-2.61-.1-.25-.43-1.24.1-2.58 0 0 .82-.26 2.67 1a9.2 9.2 0 0 1 4.86 0c1.85-1.26 2.66-1 2.66-1 .54 1.34.2 2.33.1 2.58.63.68 1 1.55 1 2.61 0 3.73-2.27 4.55-4.44 4.79.35.3.67.9.67 1.82v2.7c0 .26.17.55.67.46A9.5 9.5 0 0 0 12 2.5Z"
									/>
								</svg>
							</span>
							<span>
								<strong>GitHub</strong>
								<small>github.com/Yodasama</small>
							</span>
						</a>
						<a href={`mailto:${profile.email}`}>
							<span class="contact-icon" aria-hidden="true">
								<svg viewBox="0 0 24 24">
									<path d="M4 6.5h16v11H4z" />
									<path d="m4 7 8 6 8-6" />
								</svg>
							</span>
							<span>
								<strong>Email</strong>
								<small>{profile.email}</small>
							</span>
						</a>
					</div>
				</section>
			</aside>

			<main class="content-area">
				<nav class="top-nav" aria-label="主导航">
					<div class="nav-links">
						<a href="/">Home</a>
						<a href="/blog">Articles</a>
						<a class="active" href="/projects">Projects</a>
						<a href="/about">About</a>
					</div>
					<div class="nav-tools">
						<button class="theme-toggle" type="button" aria-label="切换明暗主题">
							<svg viewBox="0 0 24 24" aria-hidden="true">
								<path d="M12 3v2" />
								<path d="M12 19v2" />
								<path d="m4.22 4.22 1.42 1.42" />
								<path d="m18.36 18.36 1.42 1.42" />
								<path d="M3 12h2" />
								<path d="M19 12h2" />
								<path d="m4.22 19.78 1.42-1.42" />
								<path d="m18.36 5.64 1.42-1.42" />
								<circle cx="12" cy="12" r="4" />
							</svg>
						</button>
						<form class="search-form" action="/blog" method="get" role="search">
							<label class="sr-only" for="site-search">搜索文章</label>
							<svg viewBox="0 0 24 24" aria-hidden="true">
								<path d="m21 21-4.35-4.35" />
								<circle cx="11" cy="11" r="7" />
							</svg>
							<input id="site-search" name="q" type="search" placeholder="Search articles" />
						</form>
					</div>
				</nav>

				<section class="projects-panel" aria-labelledby="projects-title">
					<div class="projects-heading">
						<div>
							<p>Selected Work</p>
							<h2 id="projects-title">Projects</h2>
						</div>
						<span>{projects.length} projects</span>
					</div>

					<div class="projects-scroll">
						<div class="project-stack">
							{
								projects.map((project) => (
									<a class="project-card" href={project.href} data-categories={project.categories.join(' ')}>
										<div class="project-icon-large" aria-hidden="true">
											<svg viewBox="0 0 24 24">
												<path d="m12 3 8 4.5-8 4.5-8-4.5L12 3Z" />
												<path d="m20 12-8 4.5L4 12" />
												<path d="m20 16.5-8 4.5-8-4.5" />
											</svg>
										</div>
										<div class="project-card-main">
											<div class="project-title-row">
												<h3>{project.name}</h3>
												<span>{project.status}</span>
											</div>
											<p>{project.description}</p>
											<div class="tag-list">
												{project.tags.map((tag) => (
													<span>{tag}</span>
												))}
											</div>
										</div>
									</a>
								))
							}
						</div>
						<p class="empty-state" data-empty-state>这个分类下暂时还没有项目。</p>
					</div>
				</section>
			</main>
		</div>

		<script>
			const themeButton = document.querySelector('.theme-toggle');
			const savedTheme = localStorage.getItem('theme');

			if (savedTheme === 'dark') {
				document.body.classList.add('dark-mode');
			}

			themeButton?.addEventListener('click', () => {
				document.body.classList.toggle('dark-mode');
				localStorage.setItem('theme', document.body.classList.contains('dark-mode') ? 'dark' : 'light');
			});

			const filterButtons = [...document.querySelectorAll('.focus-button')];
			const projectCards = [...document.querySelectorAll('.project-card')];
			const emptyState = document.querySelector('[data-empty-state]');

			function applyFilter(slug) {
				const activeButton = filterButtons.find((button) => button.dataset.filter === slug);
				const activeSlug = activeButton?.dataset.filter ?? 'all';

				filterButtons.forEach((button) => {
					button.classList.toggle('active', button.dataset.filter === activeSlug);
				});

				const showCard = (card) =>
					activeSlug === 'all' || (card.dataset.categories ?? '').split(' ').includes(activeSlug);
				const visibleCards = projectCards.filter(showCard);

				projectCards.forEach((card) => {
					card.hidden = !showCard(card);
				});
				if (emptyState) emptyState.hidden = visibleCards.length > 0;

				const url = new URL(window.location.href);
				if (activeSlug === 'all') {
					url.searchParams.delete('focus');
				} else {
					url.searchParams.set('focus', activeSlug);
				}
				window.history.replaceState({}, '', url);
			}

			filterButtons.forEach((button) => {
				button.addEventListener('click', () => applyFilter(button.dataset.filter ?? 'all'));
			});

			applyFilter(new URLSearchParams(window.location.search).get('focus') ?? 'all');
		</script>
	</body>
</html>

<style>
	:global(body.projects-page) {
		min-height: 100vh;
		background:
			radial-gradient(circle at 78% 8%, rgba(72, 122, 255, 0.12), transparent 34rem),
			linear-gradient(90deg, #f5f7fb 0, #f5f7fb 19.5rem, #ffffff 19.5rem);
		color: #111827;
		font-size: 16px;
		line-height: 1.5;
		transition:
			background 180ms ease,
			color 180ms ease;
	}

	:global(body.projects-page.dark-mode) {
		background:
			radial-gradient(circle at 78% 8%, rgba(96, 165, 250, 0.16), transparent 34rem),
			linear-gradient(90deg, #0f172a 0, #0f172a 19.5rem, #111827 19.5rem);
		color: #e5e7eb;
	}

	:global(body.projects-page a) {
		color: inherit;
		text-decoration: none;
	}

	.site-shell,
	.site-shell * {
		box-sizing: border-box;
	}

	.site-shell {
		display: grid;
		grid-template-columns: 19.5rem minmax(0, 1fr);
		min-height: 100svh;
	}

	.profile-panel {
		position: sticky;
		top: 0;
		display: flex;
		flex-direction: column;
		gap: 1.35rem;
		height: 100svh;
		padding: 2rem;
		border-right: 1px solid rgba(148, 163, 184, 0.22);
		overflow: clip;
	}

	.profile-main {
		text-align: center;
	}

	.avatar {
		width: 7.5rem;
		height: 7.5rem;
		margin: 0 auto 1rem;
		border: 1px solid rgba(255, 255, 255, 0.7);
		border-radius: 999px;
		box-shadow: 0 20px 54px rgba(15, 23, 42, 0.18);
		object-fit: cover;
		object-position: center;
	}

	.profile-main h1 {
		margin: 0;
		color: #030712;
		font-size: 1.55rem;
		line-height: 1.1;
	}

	.profile-main p,
	.panel-section p,
	.project-card p {
		color: #4b5563;
	}

	.profile-main p {
		margin: 0.7rem 0 0;
		font-size: 0.92rem;
	}

	.panel-section {
		padding-top: 1.15rem;
		border-top: 1px solid rgba(148, 163, 184, 0.28);
	}

	.connect-section {
		margin-top: auto;
	}

	.panel-section h2 {
		margin: 0 0 0.65rem;
		color: #6b7280;
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.panel-section p {
		margin: 0;
		font-size: 0.88rem;
	}

	.focus-list,
	.contact-list {
		display: grid;
		gap: 0.72rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.contact-list a {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		color: #111827;
		font-size: 0.88rem;
	}

	.contact-icon,
	.theme-toggle,
	.search-form svg {
		display: grid;
		place-items: center;
		flex: 0 0 auto;
		color: #4b5563;
	}

	.contact-icon svg,
	.theme-toggle svg,
	.search-form svg {
		width: 1.05rem;
		height: 1.05rem;
		fill: none;
		stroke: currentColor;
		stroke-linecap: round;
		stroke-linejoin: round;
		stroke-width: 1.8;
	}

	.focus-button {
		display: flex;
		align-items: center;
		width: 100%;
		min-height: 2.35rem;
		justify-content: flex-start;
		padding: 0 0.7rem;
		border: 1px solid transparent;
		border-radius: 0.8rem;
		background: transparent;
		color: #111827;
		cursor: pointer;
		font: inherit;
		font-size: 0.88rem;
		font-weight: 700;
		line-height: 1;
		transition:
			background 180ms ease,
			border-color 180ms ease,
			transform 180ms ease;
	}

	.focus-button:hover,
	.focus-button.active {
		border-color: rgba(209, 213, 219, 0.78);
		background: rgba(255, 255, 255, 0.74);
		transform: translateX(2px);
	}

	.focus-button:focus {
		outline: none;
	}

	.focus-button:focus-visible {
		outline: 2px solid #2563eb;
		outline-offset: 2px;
	}

	.contact-list a {
		border-radius: 0.75rem;
		transition:
			background 180ms ease,
			transform 180ms ease;
	}

	.contact-list a:hover {
		background: rgba(255, 255, 255, 0.66);
		transform: translateX(2px);
	}

	.contact-list strong,
	.contact-list small {
		display: block;
	}

	.contact-list small {
		color: #6b7280;
		font-size: 0.78rem;
	}

	.content-area {
		display: grid;
		grid-template-rows: 2.55rem minmax(0, 1fr);
		gap: 0.9rem;
		width: 100%;
		max-width: 78rem;
		height: 100svh;
		margin: 0 auto;
		padding: 1rem 2rem 1.25rem;
		overflow: hidden;
	}

	.top-nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		min-width: 0;
	}

	.nav-links {
		display: flex;
		align-items: center;
		justify-content: flex-start;
		gap: 0.65rem;
		min-width: 0;
	}

	.nav-links a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 2.35rem;
		padding: 0 0.95rem;
		border-radius: 999px;
		font-size: 0.9rem;
		font-weight: 700;
		transition:
			background 180ms ease,
			box-shadow 180ms ease;
	}

	.nav-links a.active,
	.nav-links a:hover {
		background: rgba(243, 244, 246, 0.94);
		box-shadow: inset 0 0 0 1px rgba(148, 163, 184, 0.12);
	}

	.nav-tools {
		display: flex;
		align-items: center;
		gap: 0.7rem;
		flex: 0 0 auto;
	}

	.theme-toggle,
	.search-form {
		border: 1px solid rgba(209, 213, 219, 0.86);
		background: rgba(255, 255, 255, 0.9);
		box-shadow: 0 12px 28px rgba(15, 23, 42, 0.06);
	}

	.theme-toggle {
		width: 2.35rem;
		height: 2.35rem;
		padding: 0;
		border-radius: 999px;
		cursor: pointer;
	}

	.search-form {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		width: clamp(12rem, 20vw, 18rem);
		height: 2.35rem;
		padding: 0 0.85rem;
		border-radius: 999px;
	}

	.search-form input {
		width: 100%;
		min-width: 0;
		border: 0;
		outline: 0;
		background: transparent;
		color: #111827;
		font: inherit;
		font-size: 0.86rem;
	}

	.search-form input::placeholder {
		color: #9ca3af;
	}

	.projects-panel {
		display: flex;
		flex-direction: column;
		min-height: 0;
		border: 1px solid rgba(226, 232, 240, 0.92);
		border-radius: 1.35rem;
		background: rgba(255, 255, 255, 0.88);
		box-shadow: 0 22px 80px rgba(15, 23, 42, 0.06);
		overflow: hidden;
	}

	.projects-heading {
		display: flex;
		align-items: flex-end;
		justify-content: space-between;
		gap: 1rem;
		padding: 1.35rem 1.45rem 1rem;
		border-bottom: 1px solid rgba(226, 232, 240, 0.9);
	}

	.projects-heading p {
		margin: 0 0 0.2rem;
		color: #64748b;
		font-size: 0.78rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.projects-heading h2 {
		margin: 0;
		color: #0f172a;
		font-size: clamp(1.8rem, 3vw, 3rem);
		line-height: 1;
	}

	.projects-heading span {
		color: #64748b;
		font-size: 0.86rem;
		font-weight: 700;
	}

	.projects-scroll {
		flex: 1;
		min-height: 0;
		overflow-y: auto;
		padding: 1rem 1.05rem 1.25rem 1.45rem;
		scrollbar-gutter: stable;
	}

	.project-stack {
		display: grid;
		gap: 0.85rem;
	}

	.project-card {
		display: grid;
		grid-template-columns: 4rem minmax(0, 1fr);
		gap: 1rem;
		align-items: center;
		padding: 1.05rem;
		border: 1px solid rgba(226, 232, 240, 0.92);
		border-radius: 1rem;
		background: rgba(255, 255, 255, 0.72);
		transition:
			border-color 180ms ease,
			box-shadow 180ms ease,
			transform 180ms ease;
	}

	.project-card:hover {
		border-color: rgba(148, 163, 184, 0.6);
		box-shadow: 0 14px 38px rgba(15, 23, 42, 0.08);
		transform: translateY(-1px);
	}

	.project-card[hidden],
	.empty-state[hidden] {
		display: none;
	}

	.project-card h3 {
		margin: 0;
		color: #0f172a;
		font-size: 1.08rem;
		line-height: 1.25;
	}

	.project-card p {
		display: -webkit-box;
		margin: 0.42rem 0 0;
		overflow: hidden;
		font-size: 0.88rem;
		-webkit-box-orient: vertical;
		-webkit-line-clamp: 2;
	}

	.project-icon-large {
		display: grid;
		place-items: center;
		width: 4rem;
		height: 4rem;
		border-radius: 1rem;
		background: #dbeafe;
		color: #2563eb;
	}

	.project-icon-large svg {
		width: 1.55rem;
		height: 1.55rem;
		fill: none;
		stroke: currentColor;
		stroke-linecap: round;
		stroke-linejoin: round;
		stroke-width: 1.8;
	}

	.project-title-row {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
	}

	.project-title-row span {
		flex: 0 0 auto;
		padding: 0.18rem 0.6rem;
		border-radius: 999px;
		background: #ecfdf5;
		color: #047857;
		font-size: 0.72rem;
		font-weight: 800;
	}

	.tag-list {
		display: flex;
		flex-wrap: wrap;
		gap: 0.45rem;
		margin-top: 0.72rem;
	}

	.tag-list span {
		padding: 0.16rem 0.55rem;
		border-radius: 999px;
		background: #f3f4f6;
		color: #4b5563;
		font-size: 0.72rem;
		font-weight: 700;
	}

	.empty-state {
		display: grid;
		place-items: center;
		min-height: 12rem;
		margin: 0;
		border: 1px dashed rgba(148, 163, 184, 0.5);
		border-radius: 1rem;
		color: #64748b;
		font-size: 0.86rem;
		text-align: center;
	}

	:global(body.projects-page.dark-mode) .profile-main h1,
	:global(body.projects-page.dark-mode) .focus-button,
	:global(body.projects-page.dark-mode) .contact-list a,
	:global(body.projects-page.dark-mode) .projects-heading h2,
	:global(body.projects-page.dark-mode) .project-card h3 {
		color: #f9fafb;
	}

	:global(body.projects-page.dark-mode) .profile-main p,
	:global(body.projects-page.dark-mode) .panel-section p,
	:global(body.projects-page.dark-mode) .project-card p,
	:global(body.projects-page.dark-mode) .projects-heading p,
	:global(body.projects-page.dark-mode) .projects-heading span,
	:global(body.projects-page.dark-mode) .contact-list small,
	:global(body.projects-page.dark-mode) .empty-state {
		color: #cbd5e1;
	}

	:global(body.projects-page.dark-mode) .projects-panel,
	:global(body.projects-page.dark-mode) .theme-toggle,
	:global(body.projects-page.dark-mode) .search-form {
		border-color: rgba(71, 85, 105, 0.8);
		background: rgba(15, 23, 42, 0.78);
	}

	:global(body.projects-page.dark-mode) .project-card {
		border-color: rgba(71, 85, 105, 0.72);
		background: rgba(15, 23, 42, 0.52);
	}

	:global(body.projects-page.dark-mode) .nav-links a.active,
	:global(body.projects-page.dark-mode) .nav-links a:hover,
	:global(body.projects-page.dark-mode) .focus-button:hover,
	:global(body.projects-page.dark-mode) .focus-button.active,
	:global(body.projects-page.dark-mode) .tag-list span,
	:global(body.projects-page.dark-mode) .project-title-row span {
		background: rgba(30, 41, 59, 0.86);
		color: #e5e7eb;
	}

	:global(body.projects-page.dark-mode) .project-icon-large {
		background: rgba(37, 99, 235, 0.18);
		color: #93c5fd;
	}

	:global(body.projects-page.dark-mode) .search-form input {
		color: #f9fafb;
	}

	:global(body.projects-page.dark-mode) .projects-heading,
	:global(body.projects-page.dark-mode) .empty-state {
		border-color: rgba(71, 85, 105, 0.8);
	}

	@media (max-width: 1180px) {
		.site-shell {
			grid-template-columns: 17rem minmax(0, 1fr);
		}

		:global(body.projects-page) {
			background:
				radial-gradient(circle at 78% 8%, rgba(72, 122, 255, 0.12), transparent 34rem),
				linear-gradient(90deg, #f5f7fb 0, #f5f7fb 17rem, #ffffff 17rem);
		}

		.content-area {
			padding-inline: 1.35rem;
		}

		.search-form {
			width: 13rem;
		}
	}

	@media (max-width: 900px) {
		:global(body.projects-page),
		:global(body.projects-page.dark-mode) {
			background: #f8fafc;
		}

		:global(body.projects-page.dark-mode) {
			background: #111827;
		}

		.site-shell {
			display: block;
		}

		.profile-panel {
			position: relative;
			height: auto;
			padding: 2rem 1.1rem;
			border-right: 0;
			border-bottom: 1px solid rgba(148, 163, 184, 0.22);
			overflow: visible;
		}

		.connect-section {
			margin-top: 0;
		}

		.content-area {
			display: block;
			height: auto;
			max-width: none;
			padding: 1rem;
			overflow: visible;
		}

		.top-nav,
		.nav-links,
		.nav-tools {
			flex-wrap: wrap;
		}

		.nav-tools {
			width: 100%;
		}

		.search-form {
			flex: 1;
			width: auto;
		}

		.projects-panel {
			margin-top: 1rem;
			max-height: none;
		}

		.projects-scroll {
			overflow: visible;
		}
	}

	@media (max-width: 560px) {
		.nav-links a {
			min-height: 2.15rem;
			padding: 0 0.72rem;
			font-size: 0.8rem;
		}

		.projects-heading,
		.project-card {
			display: block;
		}

		.project-icon-large {
			margin-bottom: 0.9rem;
		}

		.project-title-row {
			display: block;
		}

		.project-title-row span {
			display: inline-flex;
			margin-top: 0.55rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		*,
		*::before,
		*::after {
			transition-duration: 0.01ms !important;
			animation-duration: 0.01ms !important;
		}
	}
</style>
